<div class="topic-index-wrapper">
  <div class="container">
    <% @topics.each do |topic|  %>
      <div class="row">
        <div class="col-md-6 col-md-offset-3">
          <div class="topic-index-single">
            <h2 class="topic-author">
              <%= topic.user.name %>
            </h2>
            <%= image_tag topic.image.url %>

            <!-- 消さない いいねの切り替えifを使っている
            <% if !topic.favorite_users.include?(current_user) %>
              <%= link_to favorites_path(topic_id: topic.id), method: :post do %>
                <i class="far fa-heart fa-3x topic-index-icon"></i>
              <% end %>
            <% else %>
              <%= link_to favorites_path(topic_id: topic.id), method: :delete do %>
                <i class="fas fa-heart fa-3x topic-index-icon"></i>
              <% end %>
            <% end %>
            -->

            <%= topic.favorites.length %>
            <%= link_to new_topic_comment_path(topic_id: topic.id) do %>
              <%= image_tag 'icons/comment.png', class: 'topic-index-icon' %>
            <% end %>
            <p class="topic-text">
            <%= topic.description %>
            </p>
            <h3 class="topic-comment-title">コメント</h3>
            <% topic.comments.each do |comment| %>
              <p>
              <%= comment.body %>
              </p>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
